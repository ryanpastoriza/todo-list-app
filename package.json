{
  "private": true,
  "name": "todo-list-app",
  "workspaces": [
    "server",
    "shared",
    "client"
  ],
  "scripts": {
    "build": "npm run build -w server",
    "start": "npm run start -w server"
  }
}